# Kube Visibility Dashboard UI

Angular front end for the Kube Visibility Dashboard.

## Development server

```bash
ng serve
```

Open `http://localhost:4200/`. The app reloads when source files change.

## Build

```bash
ng build
```

Artifacts are written under `dist/`.
